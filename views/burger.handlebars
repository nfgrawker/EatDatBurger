<h3 class = "">Burgers!</h3>

<div class="container">
  <div class="row">
    <div class="col-sm-4">

    </div>
  <ul>
      {{#each burgersTrue}}
      <li>
          <p>{{this.burger}}</p>
      </li>
      {{/each}}
  </ul>

  <form action="/" method="POST">
      <textarea type="text" name="wish"></textarea>

      <button type="submit">Submit</button>
  </form>
  </div>
</div>
